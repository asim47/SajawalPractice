import axios from "axios";
import {
} from "./actions/types";


const initialState = {
    //data
};

const reducer = (state = initialState, action) => {
    
    switch (action.type) {
        case "value":
            
            break;
    
        default:
        return state;
    }

    
}

export default reducer;
